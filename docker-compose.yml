services:
  watcher:
    build: .
    env_file:
      - .env
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./data:/app/data
    command: ["profiru-watch", "loop", "--config", "/app/config.yaml", "--interval", "300"]
